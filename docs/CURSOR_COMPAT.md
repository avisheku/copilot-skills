# Cursor compatibility (best-effort)

| Feature | Cursor | Notes |
|---------|--------|-------|
| SKILL.md skills | Partial | Install to `~/.cursor/skills` via `-Target Cursor` |
| Hooks | Varies | Use `hooks/COMPAT.ps1` manual invoke |
| Plugins | No | Use Layer B folders |
| MCP | Yes | Same `.mcp.json` profiles |

Path adapters only — no body transforms per ADR-002.
