# /do setup

1. Pack installed (`scripts/Install-CopilotSkills.ps1`).
2. Optional: `Invoke-ContextPack -PackId default` at session start.
3. MCP profile `minimal` unless `/mcp` says otherwise.

No extra deps beyond Phase 1 control plane.
