# /moa setup

1. Pack installed.
2. Profiles in `config/moa/profiles.json`.
3. Scripts: `Invoke-MoA.ps1`, `Invoke-MoAFinish.ps1`.
4. Agents: `agents/moa-proposer.agent.md`, `agents/moa-aggregator.agent.md`.

No Together API key required — uses Copilot/Claude native model switching + tip cards.
