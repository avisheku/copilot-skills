# Setup — SkillsForge

**20% Change. 80% Better.**

Full steps: [docs/HANDBOOK.md](docs/HANDBOOK.md) · Brand: [brand/](brand/)

1. Check org plugin policy or use Layer B folders.
2. Run installer (Phase 1): `scripts/Install-CopilotSkills.ps1 -Target Copilot -Layer Auto`
3. Copy `env/shared.md.example` to `env/user.md`
4. Golden path: `/mcp minimal` → `/do` tiny → `/2080`
