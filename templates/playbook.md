# Playbook template

Use with `/learn` kind `playbook` → promote → `/create` for new blocks.

## Trigger

When does this run?

## Steps

1. 
2. 

## VERIFY

```text
command:
expect:
```

## Error-map

Link to `learn/error-map/<id>.json` if failure classified.
