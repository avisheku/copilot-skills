# Anthropic tips (official docs — summarize at worker boundary)

- Use structured plans; separate ShortPlan (user) from FullPlan (workers).
- Tool use: precise parameters; no invented paths.
- Ref: https://docs.anthropic.com
