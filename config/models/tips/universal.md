# Universal model tips (workers)

- Confirm ShortPlan before implement.
- One task per step; cite files changed.
- Prefer native fork for parallel independent work.
- Restore context and MCP minimal after completion.
